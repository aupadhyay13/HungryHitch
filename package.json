{
    "name": "hungry-hitch",
    "version": "1.0.0",
    "engines": {
        "node": ">=18 <19"
      },
    "scripts": {
      "dev": "cd front-end && npm start && cd backend && npm start",
      "start": "cd backend/built && node server.js",
      "prebuild": "cd backend && npm run build",
      "build": "cd admin-frontend && npm run build"
    }
  }